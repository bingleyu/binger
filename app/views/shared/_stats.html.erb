<% @user ||= current_user %>
<div class="stats">
  <a href="<%= following_user_path(@user) %>">
    <strong id="following" class="sta center">
      <%= @user.followed_users.count %>
    </strong>
    following
  </a>
  <a href="<%= followers_user_path(@user) %>">
    <strong id="followers" class="stat center">
      <%= @user.followers.count %>
    </strong>
    followers
  </a>
  <a href="<%= user_path(@user) %>">
    <strong id="micropost_count" class="stat center">
      <%= @user.microposts.count %> 
    </strong>
    Microposts
  </a>
</div>